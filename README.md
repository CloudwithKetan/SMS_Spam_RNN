# SMS_Spam_RNN

AWS Commands

sudo apt update

sudo apt install python3-pip python3-venv nginx git -y

git clone ( Your GitHub Repository Code)

cd (repository name)

python3 -m venv venv

source venv/bin/activate

pip install -r requirements.txt

ls
python3 app.py

Expected output:

* Running on http://0.0.0.0:5000
